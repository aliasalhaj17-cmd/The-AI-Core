{
  "name": "ojin-core",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "python3 -m http.server 8000",
    "build": "echo 'Static web app ready. Use the GitHub Actions workflow to build Android APK.'"
  }
}
